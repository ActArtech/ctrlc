{
  "name": "@ctrlc/cli",
  "version": "0.1.0",
  "description": "SectionPack CLI: validate, list, pack, pack-multi, graph, scan, snapshot, watch, library, and capture for CtrlC hosts.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ctrlc": "./bin/ctrlc.mjs"
  },
  "exports": {
    ".": "./src/cli.mjs"
  },
  "files": [
    "bin",
    "dist",
    "src",
    "scripts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node ./scripts/build-cli.mjs",
    "ctrlc": "node ./bin/ctrlc.mjs",
    "test": "node ./scripts/test-cli.mjs",
    "prepublishOnly": "npm run build && node --input-type=module -e \"import fs from 'node:fs'; if (!fs.existsSync('dist/cli.mjs')) { console.error('missing dist/cli.mjs'); process.exit(1); } if (!fs.existsSync('bin/ctrlc.mjs')) { console.error('missing bin/ctrlc.mjs'); process.exit(1); }\""
  },
  "dependencies": {
    "@ctrlc/capture": "0.1.0",
    "@ctrlc/core": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  },
  "engines": {
    "node": ">=20"
  }
}
