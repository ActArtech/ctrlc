/**
 * Page content modules.
 * Add one export key per section (e.g. hero, features) and reference them
 * from section components and ctrlc register --content-key.
 */

export const siteMeta = {
  productName: "Clone",
  tagline: "",
};
