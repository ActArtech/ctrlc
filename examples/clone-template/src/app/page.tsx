/**
 * Compose sections here.
 *
 * After each section is built:
 *   1. Import the component
 *   2. Wrap with <SectionBoundary id="..." label="..." component="...">
 *   3. ctrlc register <id> --cwd . ... --from-spec docs/research/components/<id>.spec.md
 *
 * Section ids must match registry / section-pack-config.
 */
export default function HomePage() {
  return (
    <main>
      {/* compose sections here */}
    </main>
  );
}
